{# pattern_doc.md.tera — one Alexander pattern-language document per pattern.
   Generated by ggen fanout (results = [single_row] per output file).
   Users SHOULD edit these files to fill in Context/Forces/Solution/Consequences.
   The scaffold banner is NOT "DO NOT EDIT" — editing is the point.

   Available per row: id, name, family, lowering, stateCard, eventCode, eventName,
   otelSpan, requiredStatus, refusalStatus, primitive, doc, authority, objectNames.
   objectNames is comma-joined (GROUP_CONCAT); use | split(pat=",") to iterate.

   Re-scaffold (structural metadata only, overwrites): `ggen sync`
   Source of truth for narrative: this .md file (hand-edited after scaffold).
#}
{% for row in results %}
<!-- SCAFFOLDED BY ggen — fill in Context/Forces/Solution/Consequences below, then commit. -->
<!-- Source: ggen/schema/patterns.ttl ({{ row.name }}). Re-scaffold: `ggen sync`. -->

# Pattern: {{ row.eventName }}

> **Family:** {{ row.family }} · **Kernel:** `{{ row.name }}` · **Lowering:** `{{ row.lowering }}` · **Id:** {{ row.id }}

{{ row.doc }}

---

## Context

<!-- TODO: Describe the game situation that makes {{ row.eventName }} necessary.
     Why does this pattern exist? What breaks — or becomes unpredictably slow —
     without it? Seed from the authority line below, then expand:
     Authority: {{ row.authority }} -->

_Replace this placeholder with 2–3 sentences on the game problem this pattern addresses._

## Forces

<!-- TODO: List the tensions this pattern must hold in balance. Consider:
     - Branch misprediction: every {{ row.eventName }} call that branches adds jitter
     - Deterministic latency: the {{ row.lowering }} lowering gives O(1) constant time
     - Bounded state: stateCard = {{ row.stateCard }} ({{ row.stateCard }} distinct states)
     - Auditability: the OCEL event code {{ row.eventCode }} ties the transition to an object trace
     Authority to defend: {{ row.authority }} -->

_Replace this placeholder with the forces — what pulls in opposite directions here._

## Solution

<!-- TODO: Explain how {{ row.name }} resolves the forces.
     It lowers onto `{{ row.primitive }}` to compute without conditional branches.
     Describe the bit-field ABI (how state and input are packed into u64),
     the branchless arithmetic, and why `{{ row.lowering }}` was the right lowering choice. -->

**Branchless primitive:** `{{ row.primitive }}`

_Replace this placeholder with the solution description._

## Consequences

<!-- TODO: What trade-offs follow from applying this pattern?
     Gains: predictable latency, side-channel resistance, OCEL audit trail.
     Costs: fixed bit-field ABI, state space bounded to {{ row.stateCard }} classes.
     What patterns naturally compose with this one (see Related Patterns below)? -->

_Replace this placeholder with consequences and trade-offs._

---

## Structure Diagram

{% if row.lowering == "Dfa" %}
{% set state_count = row.stateCard | int %}
```mermaid
---
title: {{ row.eventName }} — DFA ({{ row.stateCard }} states)
---
stateDiagram-v2
    [*] --> S0
{% for i in range(end=state_count) %}
    S{{ i }}: State_{{ i }}
{% endfor %}
{% for i in range(end=state_count) %}
    S{{ i }} --> S{{ i }} : TODO_symbol
{% endfor %}
```

<!-- TODO: Replace State_N labels and TODO_symbol edges with the actual state names
     and alphabet symbols from src/patterns/{{ row.name }}.rs (see the DFA table
     comment and the _reference oracle for the canonical state/symbol vocabulary). -->

{% else %}
{% set obj_list = row.objectNames | split(pat=",") %}
```mermaid
graph LR
    state["state\n(u64)"]
    input["input\n(u64)"]
    kernel["{{ row.name }}\n{{ row.lowering }}: {{ row.primitive }}"]
    result["result\n(u64)"]
    state --> kernel
    input --> kernel
    kernel --> result
{% for obj in obj_list %}
    ocel_{{ loop.index0 }}["OCEL: {{ obj }}"]
    result --> ocel_{{ loop.index0 }}
{% endfor %}
```

<!-- TODO: Improve this structural data-flow diagram:
     - Annotate bit-field layout on the state/input/result nodes
     - Label the arithmetic operation on the kernel node
     - Add state machine nodes if this pattern has meaningful internal states
     - Or replace with a more specific diagram tailored to this pattern -->

{% endif %}

---

## Evidence Model

| Field | Value |
|---|---|
| OCEL activity | `{{ row.eventName }}` |
| Event code | `{{ row.eventCode }}` |
| OTEL span | `{{ row.otelSpan }}` |
| Object kinds | {% set obj_list = row.objectNames | split(pat=",") %}`{{ obj_list | join(sep="`, `") }}` |
| Required status | `{{ row.requiredStatus }}` |
| Refusal status | `{{ row.refusalStatus }}` |
| Authority | {{ row.authority }} |

---

## Metadata

| Field | Value |
|---|---|
| Pattern id | {{ row.id }} |
| Family | {{ row.family }} |
| Lowering | `{{ row.lowering }}` |
| State cardinality | {{ row.stateCard }} |
| Primitive | `{{ row.primitive }}` |
| Kernel signature | `{{ row.name }}(state: u64, input: u64) -> u64` |
| Source kernel | `src/patterns/{{ row.name }}.rs` |

---

## How to Use

```rust
use wasm4games::patterns::{{ row.name }};

// Pack state and input into u64 fields as documented in the kernel source.
let result = {{ row.name }}(state, input);
```

The kernel is a pure `fn(u64, u64) -> u64` — no side effects, no allocation, no branches.
Pair with the evidence layer to emit OCEL events, OTEL spans, and receipt folds:

```rust
use wasm4games::evidence::{ocel::OcelEvent, otel};

let result = {{ row.name }}(state, input);
otel::emit({{ row.otelSpan }});
let ev = OcelEvent::new({{ row.eventCode }}, logical_tick, admission_status);
```

<!-- TODO: Add a concrete game-loop example showing this kernel in context:
     how is the state packed? what does the caller do with the result?
     what does the OCEL event represent in the game world? -->

---

## Related Patterns

<!-- TODO: Add links to related patterns in this directory. Examples:
     - [PatternName](pattern_name.md) — brief relationship note
     Suggestions: look for patterns in the same family, same lowering, or that
     compose naturally (one pattern's output feeds another's input). -->

_No related patterns linked yet — fill in and remove this placeholder._
{% endfor %}
